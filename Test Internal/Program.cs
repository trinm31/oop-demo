using System;
using OOP;

namespace Test_Internal
{
    class Program
    {
        static void Main(string[] args)
        {
            // Caculator caculator = new Caculator();
            // caculator.Add(1, 2);
        }
    }
}